<form method="post" action="{{ path('app_comentario_delete', { 'idArticulo': articulo.id, 'id': comentario.id }) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ comentario.id) }}">
    <button class="btn btn-danger mt-2">Eliminar</button>
</form>
